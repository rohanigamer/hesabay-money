name: Build Info

on:
  push:
    branches: [main]

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Build Instructions
        run: |
          echo "============================================"
          echo "  HESABAY MONEY - BUILD OPTIONS"
          echo "============================================"
          echo ""
          echo "Option 1: Use Codemagic (FREE - 500 min/month)"
          echo "  1. Go to https://codemagic.io"
          echo "  2. Sign in with GitHub"
          echo "  3. Add this repo"
          echo "  4. Build automatically detects codemagic.yaml"
          echo ""
          echo "Option 2: Use EAS Build (30 free builds/month)"
          echo "  Run: eas build --platform android --profile preview"
          echo ""
          echo "============================================"
